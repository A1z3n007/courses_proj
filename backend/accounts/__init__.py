"""
The accounts app provides API endpoints for user registration and profile
management. It relies on Django's built-in User model and uses
Django REST Framework serializers and views for CRUD operations.
"""